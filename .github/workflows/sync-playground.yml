name: Sync to Playground

on:
  push:
    branches:
      - main

jobs:
  build-and-push-on-push:
    uses: lubusIN/.github/.github/workflows/sync-playground.yml@main
    with:
      target-branch: playground
      force-folder: |
        build/
        vendor/
    secrets: inherit
